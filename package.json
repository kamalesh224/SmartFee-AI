{
  "name": "smartfee-ai",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx --prefix frontend vite --host",
    "build": "npx --prefix frontend vite build",
    "lint": "npx --prefix frontend oxlint",
    "preview": "npx --prefix frontend vite preview"
  }
}
